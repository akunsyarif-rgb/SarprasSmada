/**
 * UtilityService.gs — PLACEHOLDER
 *
 * Modul ini akan berisi fungsi bantu lintas domain yang tidak spesifik
 * terhadap satu domain bisnis tertentu (mis. format tanggal/timestamp,
 * validasi format umum, pembuatan objek response standar untuk frontend).
 *
 * Belum ada implementasi pada tahap PHASE 1 (Repository Foundation).
 * Implementasi dijadwalkan pada PHASE 2 — Core Backend.
 * Lihat docs/ARCHITECTURE.md (bagian CORE — Utility) dan
 * docs/DEVELOPMENT_ROADMAP.md.
 *
 * Cakupan yang direncanakan:
 * - Formatter timestamp yang konsisten untuk seluruh sheet
 *   (created_at, updated_at, dan kolom waktu lainnya).
 * - Pembuatan struktur response sukses/gagal yang seragam.
 * - Fungsi validasi umum (format email, string kosong, dsb.).
 */
